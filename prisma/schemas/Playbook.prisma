model Playbook {
  id          String   @id @default(cuid())
  gameId      String
  name        String
  version     Int
  template      Json
  createdAt   DateTime @default(now())
  description String?

  game       Game        @relation(fields: [gameId], references: [id])
  characters Character[]

  @@unique([gameId, name, version])
}
